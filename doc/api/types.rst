Protocol Types
==============

Types shared across the *libedhoc* API: the protocol enumerations of
:term:`RFC 9528` (message number, role, method), the byte-sequence view used
throughout the interfaces, the context passed to application callbacks, and the
EDHOC error reporting types.

| Header file: :file:`include/edhoc/types.h`

.. doxygengroup:: edhoc-types
   :project: libedhoc
   :members:
